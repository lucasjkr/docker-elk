output {
     if "filebeat" in [tags] {
         elasticsearch {
             hosts => ["elasticsearch:9200"]
             manage_template => false
        # Uncomment one of the two lines below for daily vs monthly indexes
        #     index => "apache2-%{+YYYY.MM}"
             index => "apache2-%{+YYYY.MM.dd}"
         }
     }
}