filter {
    if "filebeat" in [tags] {
        grok {
            match => {
                "message" => "%{COMBINEDAPACHELOG}"
            }
        }
        geoip {
            database => "/etc/maxmind/GeoLite2-City.mmdb"
            source => "clientip"
        }
    }
}