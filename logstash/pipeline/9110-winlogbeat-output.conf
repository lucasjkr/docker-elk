output {
  if "winlogbeat" in [tags] {
    elasticsearch {
      hosts => ["elasticsearch:9200"]
      manage_template => false
# Uncomment one of the two lines below for daily vs monthly indexes
#      index => "winlogbeat-6.4.0-%{+YYYY.MM}"
      index => "winlogbeat-6.4.0-%{+YYYY.MM.dd}"
    }
  }
}