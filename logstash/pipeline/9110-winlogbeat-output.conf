output {
  if "winlogbeat" in [tags] {
    elasticsearch {
      hosts => ["elasticsearch:9200"]
      manage_template => false
      index => "winlogbeat-6.3.2-%{+YYYY.MM}"
#      index => "winlogbeat-6.3.2-%{+YYYY.MM.dd}"
    }
  }
}